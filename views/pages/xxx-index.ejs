<style>
    #xxx-menu-container {
        width: 800px;
        padding: 40px;
        background: rgba(0, 0, 0, 0.6);
        backdrop-filter: blur(15px);
        border-radius: 24px;
        border: 1px solid rgba(255, 255, 255, 0.1);
        display: flex;
        flex-direction: column;
        align-items: center;
        box-shadow: 0 20px 50px rgba(0,0,0,0.5);
    }

    .xxx-header {
        font-family: 'Fira Sans', sans-serif;
        font-size: 1.5em;
        color: #fff;
        text-transform: uppercase;
        letter-spacing: 4px;
        margin-bottom: 40px;
        border-bottom: 2px solid #5ab565;
        padding-bottom: 10px;
    }

    .xxx-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 20px;
        width: 100%;
        margin-bottom: 40px;
    }

    .xxx-category-card {
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 16px;
        padding: 30px 20px;
        text-align: center;
        cursor: pointer;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        display: flex;
        flex-direction: column;
        gap: 10px;
    }

    .xxx-category-card:hover {
        background: rgba(90, 181, 101, 0.2);
        border-color: #5ab565;
        transform: translateY(-5px);
        box-shadow: 0 10px 20px rgba(90, 181, 101, 0.2);
    }

    .xxx-category-card h3 {
        margin: 0;
        color: #ff9d00; /* Subtle orange highlight for titles */
        font-size: 1.4em;
        letter-spacing: 1px;
    }

    .xxx-category-card p {
        margin: 0;
        color: rgba(255, 255, 255, 0.6);
        font-size: 0.9em;
    }

.close-menu-btn {
        position: absolute;
        top: 25px;
        right: 25px;
        width: 45px;
        height: 45px;
        background: rgba(255, 255, 255, 0.05);
        border: 1px solid rgba(255, 255, 255, 0.2);
        border-radius: 50%;
        color: white;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: 0.2s;
        z-index: 10;
    }

.close-menu-btn:hover {
        background: #ff0000;
        border-color: #ff0000;
        transform: scale(1.1);
    }
</style>

<div id="xxx-menu-container">

    <div class="close-menu-btn" onclick="closeSubMenu()">
        <span style="font-size: 25px;" class="material-symbols-rounded">close</span>
    </div>

    <div class="xxx-header">XXX Content</div>

    <div class="xxx-grid">
        <div class="xxx-category-card" onclick="handleNav('/xvideos-grid')">
            <h3>Hardcore</h3>
            <p>Direct Grid Browser</p>
        </div>

<div class="xxx-category-card" onclick="handleNav('/xvideos-grid?tag=second life')">
    <h3>Sex on Second Life</h3>
    <p>Premium Quality Streams</p>
</div>

        <div class="xxx-category-card" onclick="handleNav('/erotic-grid')">
            <h3>Erotic Movies</h3>
            <p>Full Length Cinema</p>
        </div>

        <div class="xxx-category-card" onclick="handleNav('/xxx-browsers')">
            <h3>Porn Links</h3>
            <p>External Web Directory</p>
        </div>
    </div>

</div>

<script>
    function goToX(mode) {
        let path = '';
        
        if(mode === "xvideos") path = '/xvideos-grid'; // Matches your XV route
        if(mode === "eporner") path = 'eporner'; 
        
        // NEW: Link the "Erotic Movies" button to your new scraper route
        if(mode === "eroticMovies") path = '/erotic-grid'; 
        
        if (typeof handleNav === 'function' && path !== '') {
            handleNav(path);
        }
    }
</script>