<style>
#adult-warning-wrapper {
    width: 700px;
    padding: 40px;
    background: radial-gradient(circle at center, rgba(30, 10, 10, 0.75), rgba(0, 0, 0, 0.4));
    border-radius: 25px;
    border: 1px solid rgba(255, 50, 50, 0.25);
    display: flex;
    flex-direction: column;
    align-items: center;
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.9);
    margin: 20px auto;
    position: relative;
    font-family: Arial, sans-serif;
    text-align: center;
}

.adult-close-btn {
    position: absolute;
    top: 15px;
    right: 18px;
    cursor: pointer;
    color: white;
    font-size: 24px;
    user-select: none;
}

.adult-close-btn:hover {
    color: #ff4d4d;
}

.adult-title {
    color: #ff3b3b;
    font-size: 30px;
    font-weight: bold;
    text-transform: uppercase;
    letter-spacing: 3px;
    margin-bottom: 25px;
}

.adult-message {
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.08);
    border-radius: 14px;
    padding: 20px 25px;
    margin-bottom: 25px;
    width: 100%;
    box-sizing: border-box;
}

.adult-message p {
    margin: 8px 0;
    font-size: 22px;
    line-height: 1.5;
    color: #e0e0e0;
}

.adult-message .access-line {
    color: #4ecb71;
}

.adult-message .hint-line {
    color: #f4c542;
    font-size: 20px;
}

.password-box {
    width: 100%;
    display: flex;
    gap: 10px;
}

.password-box input[type="password"] {
    flex-grow: 1;
    background: rgba(0, 0, 0, 0.5);
    border: 1px solid #ff3b3b;
    border-radius: 10px;
    color: white;
    font-size: 18px;
    padding: 14px 16px;
    outline: none;
    box-sizing: border-box;
}

.password-box input[type="password"]::placeholder {
    color: #888;
}

.password-box input[type="button"] {
    background: #c0392b;
    color: white;
    border: none;
    padding: 14px 30px;
    border-radius: 10px;
    cursor: pointer;
    font-weight: bold;
    font-size: 16px;
    transition: transform 0.2s;
}

.password-box input[type="button"]:hover {
    background: #ff3b3b;
    transform: scale(1.03);
}
</style>

<div id="adult-warning-wrapper">

    <div class="close-menu-btn" onclick="closeSubMenu()">
        <span class="material-symbols-rounded">close</span>
    </div>

    <div class="adult-title">Adult Content</div>

    <div class="adult-message">
        <p>The following pages are intended for adult viewers only.</p>
        <p class="access-line">Enter the password to continue, or click Back to return.</p>
        <p class="hint-line">Check the TV info Notecard for the password.</p>
    </div>

    <div class="password-box">
        <input id="adult-password" type="password" placeholder="Enter password">
        <input type="button" value="ENTER" onclick="submitAdultPassword()">
    </div>

</div>

<script>


function submitAdultPassword() {
    const pass = document.getElementById("adult-password").value.trim();
    // existing password-check logic goes here
}

document.getElementById("adult-password").addEventListener("keydown", function(event) {
    if (event.key === "Enter") {
        submitAdultPassword();
    }
});
</script>