<!DOCTYPE html>

<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />

<title>Eporner Menu</title>

    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="./../../js/myScripts.js"></script>
	

<style>
  
    
body{
    background: black;
    font-family: Charcoal, Charcoal, sans-serif;
}

p{
    justify-content: center;
    align-content: center;
    align-items: center;
    text-align: center;
    font-size:46px;
    left:0%;
    color:white;
    line-height:200%;
}

 #header{
                    position: fixed;
                    top: 15px;
                    left: 75px;
                    width: 1134px;
                    height: 649px;
                    background: rgba(50, 50, 50, 0.67);
                    border: 3px solid white;
                    border-radius: 8px;
                    z-index:10000;
}

.btn, label {
    
     background:#803b61;
     border: 5px solid #dfe3d6;
     color: white;
     border-radius: 12px;
     padding: 10px 20px;
     text-align: center;
     text-decoration: none;
     display: inline-block;
     font-size: 22px;
     cursor:pointer;
     margin:5px;
     z-index:1000000000;
}
.btn2 {
    
    background:#803b61;
    border: 5px solid #dfe3d6;
    color: white;
    border-radius: 12px;
    padding: 11px 15px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 22px;
    cursor:pointer;
    margin:5px;
    z-index:1000000000;
}
#countInfo {
                  display:none;
                  position:fixed;
                  top:675px;
                  left:135px;
                  font-size:20px;
                  width:250px;
                  background: rgba(100, 100, 100, 0.5);
                  border: 2px solid white;
                  border-radius: 8px;
                  color:gold;
                  height: 34px;
                  pointer-events: none;
                }


#menu{
          position:fixed;
          top:675px;
          left:10px;
          border: 2px solid white;
          border-radius:8px;
          text-align: center;
          text-decoration: none;
          line-height:30px;
          font-size: 20px;
          width:150px;
          height:30px;
          background: rgba(100, 100, 100, 0.5);
          color:gold;
          cursor:pointer;
}
#menu:hover{
            background:yellowgreen;
            color:black;
}

.buinfo{
          background:#b76a94;
          border: 5px solid #dfe3d6;
}

.subbut{
         background:#5f7d22;
}
.subbut:hover{
         background:yellowgreen;
         color: black;
}



#userBut{
          position:fixed;
          top:280px;
          font-size:40px;
}
#groupBut{
          position:fixed;
          top:360px;
          font-size:40px;
}
#tagBut{
          position:fixed;
          top:390px;
          font-size:40px;
}
#infoBut{
          display:none;
          position:fixed;
          top:220px;
    left: 0;
    right: 0;
    margin-left: auto;
    margin-right: auto;
    width: 850px;
          font-size:35px;
          cursor:context-menu;
           padding: 10px 40px;
}
#infoBut:focus {outline:0;}

#orderBut{
          display:none;
          position:fixed;
          top:355px;
          left:255px;
          font-size:30px;
          width:180px;
          padding:6px ;
          color:white;
}
#orderBut:focus {outline:0;}

#dateBut{
          display:none;
          position:fixed;
          top:355px;
          left:455px;
          font-size:30px;
          width:180px;
           padding:6px ;
           color:white;
}
#dateBut:focus {outline:0;}

#durationBut{   
          display:none;
          position:fixed;
          top:355px;
          left:655px;
          font-size:27px;
          width:180px;
           padding:6px ;
           color:white;
}
#durationBut:focus {outline:0;}

#hdBut{   
          display:none;
          position:fixed;
          top:355px;
          left:855px;
          font-size:30px;
          width:180px;
           padding:6px ;
           color:white;
}
#hdBut:focus {outline:0;}

#orderBut:hover, #dateBut:hover, #durationBut:hover, #hdBut:hover{
         background:yellowgreen;
         border: 5px solid grey;
}
.filter{
            display:none;
            color:yellow;
            font-size:22px;
            margin:0px;
            position:fixed;
            top:325px;
            cursor:pointer;
         
}
#tag{
          display:none;  
          position:fixed;
          height:50px;
          top:508px;
          left:370px;
          font-size:28px;
          border: 3px solid yellowgreen;
          border-radius: 12px;
          z-index:1000000;

}
#submit{
          display:none;
          position:fixed;
          top:500px;
          left:725px;
          font-size:33px;
}
#sugestBut{
          display:none;
          position:fixed;
          top:500px;
          left:890px;
          font-size:33px;
}
.userbu{
          display:none;
          width: 500px;
          left: 0;
          right: 0;
          margin-left: auto;
          margin-right: auto;
          background:#803b61;
          border: 5px solid #dfe3d6;
}
.userbu:hover{
         background:yellowgreen;
         border: 5px solid grey;
}
.mainbu{
          width: 500px;
          left: 0;
          right: 0;
          margin-left: auto;
          margin-right: auto;
          background:#803b61;
          border: 5px solid #dfe3d6;
        
}
.mainbu:hover{
         background:yellowgreen;
         border: 5px solid grey;
}

.groupbu{
          display:none;
          width: 500px;
          left: 0;
          right: 0;
          margin-left: auto;
          margin-right: auto;
          background:grey;
          border: 5px solid yellowgreen;
}
.groupbu:hover{
         background:yellowgreen;
         border: 5px solid grey;
}


.backBut:focus {
              outline:0;
}

/* FILTER DROP MENUS */
#favList1{
            z-index:10000;
            position:fixed;
            top:420px;
            left:270px;
            width:150px;
            height:30px;
            font-size:21px;
            background: DodgerBlue;
}
#favList2{
            z-index:10000;
            position:fixed;
            top:420px;
            left:470px;
            width:150px;
            height:30px;
            font-size:21px;
            background: DodgerBlue;
}

#favList3{
            z-index:10000;
            position:fixed;
            top:420px;
            left:670px;
            width:150px;
            height:30px;
            font-size:21px;
            background: DodgerBlue;
}
#favList4{
            z-index:10000;
            position:fixed;
            top:420px;
            left:870px;
            width:150px;
            height:30px;
            font-size:21px;
            background: DodgerBlue;
}


.bt{
    font-size:20px;
     border: 3px solid yellowgreen;
}
.bt:hover{
           color:black; 
           background:cyan;
}
#buttons{
            display:none;
            position:fixed;
            top:150px;
            left:90px;
            width:1175px;
            height:510px;
            justify-content: center;
            align-content: center;
            align-items: center;
          
            overflow-y: auto;
}
#conteiner{
        position:fixed;
        top:55px;
        left:25px;
        width:1210px;
        height:607px;
        background:transparent;
 
        overflow: scroll;
        overflow-x: hidden;
}
#output{
      
        position:relative;
        top:-6px;
        left:50px;
        width:1200px;
        height:510px;
        background:transparent;

}
/* width */
::-webkit-scrollbar {
  width: 12px;
}

/* Track */
::-webkit-scrollbar-track {
  box-shadow: inset 0 0 5px grey; 
  border-radius: 10px;
}
 
/* Handle */
::-webkit-scrollbar-thumb {
  background: red; 
  border-radius: 10px;
}

/* Handle on hover */
::-webkit-scrollbar-thumb:hover {
  background: yellowgreen; 
}


#title{
                    display: none;
                    position: fixed;
                    top: 12px;
                    left: 82px;
                    width: 1118px;
                    color: gold;
                    font-size: 26px;
                    border: 2px solid white;
                    border-radius: 8px;
                    background: rgba(100, 100, 100, 0.5);
                    text-align: center;
                    pointer-events: none;
}
.backBut{
              font-family: Charcoal, Charcoal, sans-serif;
              position:fixed;
              top:675px;
              left:10px;
              border: 2px solid white;
              border-radius:8px;
              text-align: center;
              text-decoration: none;
              line-height:30px;
              font-size: 20px;
              width:80px;
              height:30px;
              background: rgba(100, 100, 100, 0.5);
              color:gold;
              cursor:pointer;
        }
        .backBut:hover{
                    background:yellowgreen;
                    color:black;
        }
#group{
          display:none;
          position:fixed;
          width:280px;
          top:500px;
          left:500px;
          font-size:33px;

}
#catBack{
          background:yellowgreen;
          color:black;
           border: 3px solid white;
}
#catBack:hover{
          background:green;
          color:yellow;
}

.loader {
  display: none;
  position: fixed;
  top: 320px;
  left: 600px;
  border: 16px solid #f3f3f3;
  border-radius: 50%;
  border-top: 16px solid #3498db;
  width: 80px;
  height: 80px;
  -webkit-animation: spin 2s linear infinite; /* Safari */
  animation: spin 2s linear infinite;
}

/* Safari */
@-webkit-keyframes spin {
  0% { -webkit-transform: rotate(0deg); }
  100% { -webkit-transform: rotate(360deg); }
}

@keyframes spin {
  0% { transform: rotate(0deg); }
  100% { transform: rotate(360deg); }
}
</style>	
</head>

<body>
 
<div id="header" >

    <p style="position:fixed;top:-30px;left:400px;color:gold;font-size:50px;">Eporner Public Videos</p>
    <p style="position:fixed;top:52px;left:200px;font-size:30px;">the videos on this slideshow are provide by the public Eporner feed</p>
       
    <hr style="position:fixed; top:135px;width: 970px;left:165px;color:white;"> 
    <hr style="position:fixed; top:136px;width: 970px;left:165px;color:white;">   
           

<!-- INFO TXT FIELD -->
<input id="infoBut"   type="button"  class="btn buinfo" value="chose filter options and submit your tags" />
       
<!-- MAIN BUTTONS + INFO BUTTONS -->       
       <input id="userBut"   onclick="callMode='tag';help();"  type="button"  class="btn mainbu" value="Suggested Tag´s" />
    
   <!--    <input id="groupBut"   style="background:rgba(255, 0, 0, 0.2);pointer-events: none;;" type="button"  class="btn mainbu" value="User Channel's" /> -->
    
       <input id="tagBut"    onclick="openSub('tag');"   type="button"  class="btn mainbu" value="Tag's Search" />

<!-- TOPICS DIV DINAMIC -->  
      <div id="buttons"></div>


<!-- INPUT AND SUBMITE FIELD -->
       <input id="tag" type="text" name="query" class="sub"   />
       <input id="submit" onclick="submit();"  type="button" value="Submit" class="btn sub subbut" />
    
        <input id="sugestBut" onclick="help();" type="button"  class="btn  sub subbut" value="Suggested users" />
        <input id="group" onclick="submit2();" type="button"  class="btn  sub subbut" value="Start Search" />

<!-- END HEADER DIV -->
       </div>
<!-- OVERPAGE TXT INFO -->    
       <input id="title" class="backBut"  />
     
<!-- SEARCH AND EXIT BUTTONS -->            
<div id="menu" style="left:562px;"          onclick="menu()"  class="backBut ">HIDE MENU</div> 
<div id="back" style="left:1125px;width:80px;" onclick="back()"        class="backBut">BACK</div> 
<!-- <button  id="butInfo" class="backBut" style="width:340px;left:750px; top:680px;font-size:21px;"> use Menu to search options</button> -->

<input id="countInfo"  type="button"  class="backBut" value="videos not found" />
        
         <div id="loader" class="loader"></div>  
             
<!-- GALLERY DISPLAY DIV DINAMIC -->    
        <div id="conteiner">
       <div id="output" >
           </div>
           


      
<script>

var pageArray=[];
var dataArray=[];
var keyApi ='AIzaSyBheJJ58YA7kplGUG3dRg0BZNTW0oM_epI';  // youtube api Key


//// SESSION VARIABLES RETRIVE /////////////////////////////////////////////////
         check_browser();
         var pic = localStorage.getItem("pic");
         document.body.style.backgroundImage = "url("+pic+")";
        
         var url1 = localStorage.getItem("url1");
         var url2 = localStorage.getItem("url2");
         var return_url=url1+"&"+url2
 //////////////////////////////////////////////////////////////////////////////

/////////////////////////////////////////////////////////////////////////////////
// FUNCTION TO GET THE ATRIBUTES PASSED ON THE URL
var queryString = decodeURIComponent(window.location.search); //parsing 
      queryString = queryString.substring(1); 
      var queries = queryString.split("&"); 
    
      var from =(queries[0]); // GET THE VIDEO ID FROM THE URL
      var scroll =(queries[1]); // GET THE VIDEO ID FROM THE URL

      clear_url();

       if(scroll != 'user' && scroll !='list') updateFlag=0;
       if(scroll === 'mix') {updateFlag=0;get_Svideos('concerts');}
      
       if(scroll==='user') {updateFlag=0;get_Svideos(from);}
       if(scroll==='list') {updateFlag=0;get_Slists(from);}
///////////////////////////////////////////////////////////////////////////////////////
// MENU BUTTON GET BACK TO INDEX2.HTML
function back()
{
       window.open("./../../xxx-index?"+return_url, "_self");
}

//////////////////////////
startSetup();

function startSetup() {
        $('#menu').text('HIDE MENU')

        $("#header").show();
        $('#countInfo').hide();
        $('#buttons').hide();
        $('#title').hide();
        readVids("file"); // startup loader with saved txt file
}
///////////////////////////////////////////////////////////////////////////////////////
// HIDE/SHOW INFO PAGE 
function menu(){
            
            if($("#menu").text()==="HIDE MENU") 
            {
              $("#menu").text("SHOW MENU");
              $('#countInfo').show();
              $('#title').show();
              $("#header").hide();
            }
              else
            { 
              $("#menu").text("HIDE MENU");
              $('#countInfo').hide();
              $('#title').hide();
              $("#header").show();
            }              
               $("#topics").empty();
               clear();
      
}
// RESETS THE SEARCH VIEW /////////////////////////////////////////////
function clear(){
             
             $('#infoBut').hide();          // hide info txt
             $('.sub').hide();              // hide submit
             $('.buinfo').hide();           // hide order buttons 
             $('.filter').hide();           // hide filter txt
             $('.mainbu').show();           // show buttons
             $('#buttons').hide();           // show user sub menu
             //    $('').show();           // show buttons

}// clear() end


// FUNCTION TO CALL TVYTUBE SENDING VIDEOID, BACKPIC E SCROLL POSITION 
function sendVideo(vidId){

var scrollPos = ($("#conteiner").scrollTop() );
var roomName="teste";
window.open("./tveporn?"+vidId+"&"+pic+"&"+scrollPos+"&"+roomName, "_self");

 
}
///////////////////////////////////////////////////////////////////////////////////////
function openSub(type){ 
               
               $('.sub').show();
              
               if(type==="tag")    {callMode="tag";
                                    $("#header").show();
                                    $('.mainbu').hide();    // hide main buttons
                                 
                                    $('#infoBut').val(' submit your tags words separated by space');  
                                    $('#infoBut').show();
                                    $('#tag').attr("placeholder", "insert tag words");
                                    $('#tag').val("");
              
                                    $("#infoBut").  css({ top: 270 });
                                    $("#tag").      css({ top: 410, left: 230});
                                    $("#submit").   css({ top: 400, left: 580});
                                    $("#sugestBut").css({ top: 400, left: 745});
                                    $('#own').hide();    // hide main buttons
                                    $('#fav').hide();    // hide main buttons
                                    $('#group').hide();    // hide main buttons
                                    $("#sugestBut").val('Suggested Tags');
                                   }
                 if(type==="main")    {
                                      callMode="main";
                                      $('#tag').hide();
                                       $('#submit').hide();
                                        $('#sugestBut').hide();
                                        $('#infoBut').hide();
                                         $('.mainbu').show(); 
                                         $('#buttons').hide();
                                           $("#header").show();
                 }                   
}
//////////////////////////////////////////////////////////////////////////////
// ADD TOPICS BUTTONS  ///////////////////////////////////////////////////////

function suggestBut(){
              
                  catego=[];            // limpa array de categorias
               
                  $('.mainbu').hide();  // esconde filter txt
                  $('.buinfo').hide();  // esconde filter buttons
                  $('#buttons').show();  // mostra os botoes dos topicos
                  $('.sub').hide();  // esconde filter buttons
                  $('#sugestBut').hide();  // esconde filter buttons
                
                   readCat();            // le categorias do ficheiro txt e adiciona os botoes

}
//////////////////////////////////////////////////////////////////////////////
// ADD TOPICS BUTTONS  ///////////////////////////////////////////////////////

function help(){
              
              catego=[];            // limpa array de categorias
             $('.mainbu').hide();    // hide main buttons
              $('.filter').hide();  // esconde filter txt
              $('.buinfo').hide();  // esconde filter buttons
              $('#buttons').show();  // mostra os botoes dos topicos
              $('.sub').hide();  // esconde filter buttons
              $('#sugestBut').hide();  // esconde filter buttons
            
               readCat();            // le categorias do ficheiro txt e adiciona os botoes
  
}
var catego=[];

function readCat(){
 fetch('./catego.txt')
        .then(response => response.text())
        .then(data => {
                          console.log(JSON.parse(data));
                        addSearchBtns(JSON.parse(data));
                       });
}


function addSearchBtns(topic) {

  $("#buttons").html("");
  for (i = 0; i < topic.length; i++) {
       var but = "butx"+i;
    var $button = $("<input type='button' class='btn2 bt' />");
    $button.val(topic[i]);
    $("#buttons").append($button);
    $button.attr('id',but); 
    $("#buttons").append($button);
}
    $button.attr('id',"catBack"); 
    $button.val("BACK");
    $("#buttons").append($button);
    $("#buttons").show();
}
////////////////////////
 $(document).on("click", ".bt", function() {

      if ($(this).hasClass("bt")) {
       var query = $(this).val();
       
       $("#tag").val(query);
       $("#buttons").hide();
        $("#topics").hide();

        if(query==="BACK" ) {openSub('main');}  
        else   searchTag(query);

       
      }
});
function submit(){
                   var tag= $("#tag").val();
                   searchTag(tag);
                   
    
}
//////////////////////////////////////////////////////////////////////////////

//// FIM DA FUNÇAO PROC  ////////////////////////////////////////////////////////////////////////////////////////////////////////
var proc= function(data, title) {
                                  
                                  $("#output").empty();
                             
                                  
                                var videos=data.videos;
                      
                                var numVideos=videos.length;
                    
                        saveTitle(title,numVideos);
                       $('#countInfo').val(numVideos + ' videos found');
           
              //  console.log(videos);
              
    
                $.each(videos, function(i, item){
    
   
                    var thumb = item.thumbs[9].src;
                    var title = item.title;
                    var vid = item.id;
                    var desc = "";
                  
   
    
    
    // add thumbs div
    count ++;
    var div = document.createElement("div");
    div.id='id'+count;
    div.style.position='relative';
    div.style.float = "left";
    div.style.display='inline';
    div.className="thumbs";
    
    document.getElementById("output").appendChild(div);
    //////
    //add title div
    title=title.substring(0,80)
    var div2 = document.createElement("div");
    div2.innerHTML=title
    
    div2.style.position='absolute';
    div2.className="Vtitle";
    div2.style.textAlign= 'center';
    div2.name=vid;
    div2.style.background= '#1b171ca3';
    div2.style.color= 'white';
    div2.style.cursor = "pointer";
    //div2.style.zIndex = "1000";
    
    // add function sendVideo to title div
    div2.addEventListener("click", function(e) {
                                 sendVideo (e.target.name);
                    })
    
    document.getElementById('id'+count).appendChild(div2);
    ///////
    // add main image img
    var img = document.createElement("img");
    
    
                                  m2--;img.style.width = "360px";
                                  div2.style.top= '205px';
                                  div2.style.left= '15px';
                                  div2.style.width= '370px';
                                  div2.style.height= '30px';
                                  div2.style.fontSize='15px';
  
   
                img.style.height = "230px";
                img.style.margin = "6px 8px";
                img.style.cursor = "pointer";
                img.src = thumb ;
                img.style.border = "2px solid white";
                img.alt=vid;
                img.className="image";
               // add function sendVideo to title div  
                img.addEventListener("click", function(e) {
                                 sendVideo (e.target.alt);
                    })
                     
               document.getElementById('id'+count).appendChild(img); 
              
    
                });
    
           $('.image').show();
           $('.Vtitle').show();
    
         
    // if call comes from main set scrool to zero e comes from player set scroll to saved scroll
    console.log("FROM " + from)
    
           if(from==="main") {         
           // $("#header").show();
               $("#topics").empty();
            
                         $('#conteiner').scrollTop(0)
           }
           if(from==="player") {  
            console.log("SCROLL " + scroll) 
        //    $('#menu').text("SHOW MENU");
            $("#header").hide();
            $("#topics").empty();
             
             menu();
          
             $('#conteiner').scrollTop(scroll)
           }
        
     
          };
    
    // hover functions, scale and change border color on mouse hover
    $(document).on("mouseenter", ".thumbs", function() {
        $(this).css('margin','0px'); 
        $(this).css('transform', 'scale(' + 1.06 + ')');
        
        var id=($(this).attr('id'));
        $(this).children('img').css({ "border": "2px solid greenyellow"});
    
    
        img = document.createElement("img");
        img.style.position='absolute';
        img.className="playButton";
        img.style.textAlign= 'center';
        img.style.top='100px';
        img.style.left='180px';
        img.style.zIndex='1000';
        img.alt=$(this).children('img').attr('alt');
        img.style.cursor = "pointer";
        img.src = './../../../images/playButton.png' ;
    
        img.addEventListener("click", function(e) {
                                 sendVideo (e.target.alt);
                    })
    
    
        document.getElementById(id).append(img); 
       
    });
    // reset values on mouse leave
    $(document).on("mouseleave", ".thumbs", function() {
        
       $(this).children('img').css({ "border": "2px solid white"});
        $(this).css('transform', 'scale(' + 1 + ')');
        $('.playButton').remove();
    });
    ///////////////////////////////////////////////////////////////
    var count=0;
    var x=0;
    var m1=0;
    var m2=0;
    var m3=0;
//// FIM DA FUNÇAO PROC  ////////////////////////////////////////////////////

//// INICIO DA FUNÇAO SEARCH_USER /////////////////////////////////////////// 
// popular();

function popular()
{
    
                           var tag=$('#tag').val();
            var options={
                         query:"all",   // tag
                         per_page:150,      // num results returned
                         page:0,            // num page
                         thumbsize:"big",   // small,meddium,big
                         format:"json",     // json, xml   
                         gay:0,             // 0-no gay 1-mix 2-only gay
                         ql:0,              // 0-just hi quality 1-mix 2-just lo
                         order:"most-popular"  // top-weekly,top-monthly,most-popular,top-rated,latest, longest,shortest
            }


            id_api= "https://www.eporner.com/api/v2/video/search/";

                          $.getJSON(id_api,options,function(data) {
                                                                // console.log(data);
                                                                   user_id=data.count;                // GET USER ID 
                                                                   stat=data.stat;                      // GET STAT   
                                                            
                                                                 console.log("State: " + stat);       // IMPRIME STAT
                                                                 console.log("User Id: " + user_id);  // IMPRIME USER ID
                                                                 
                                                                 proc(data);
                              });

                          $('#title').val('search results for Popular Videos' );
                          $('#title').show();
}
function searchTag(tag)
{

                        $("#header").hide();
            var options={
                         query:tag,   // tag
                         per_page:250,      // num results returned
                         page:0,            // num page
                         thumbsize:"big",   // small,meddium,big
                         format:"json",     // json, xml   
                         gay:0,             // 0-no gay 1-mix 2-only gay
                         ql:0,              // 0-just hi quality 1-mix 2-just lo
                         order:"most-popular"  // top-weekly,top-monthly,most-popular,top-rated,latest, longest,shortest
            }

        


            id_api= "https://www.eporner.com/api/v2/video/search/";

                          $.getJSON(id_api,options,function(data) {
                                                                // console.log(data);
                                                                   user_id=data.count;                // GET USER ID 
                                                                   stat=data.stat;                      // GET STAT   
                                                            
                                                                 saveTxt(JSON.stringify(data));
                                                                 
                                                             //    readVids("load");
                $('#conteiner').show(); 
                $('#loader').hide();
                $('#menu').text("SHOW MENU");
                $('#menu').show();
               // $('#countInfo').val("NUM VIDEO: " + videos.length); // hide menu button
                  $('#countInfo').show()


                  var title='search results for Tag: '+ tag;
                 
                         
                          $('#title').val( title);
                          $('#title').show();         

                                                                 proc(data, title);
                              });
                         

 }



 
function readVids(mode) {
                              if(mode==="file") readTitle(); // if read from file read title too
                                  fetch('./eporner.txt')
                                   .then(response => response.text())
                                   .then(data => {
                                                 //   console.log(data);
                                                    var videos = JSON.parse(data);
                                      
                                        proc(videos);
                                    });
}
function saveTitle(title,numVideos){  
                          $.ajax({
                          type: 'POST',
                          url: '/saveEpornerTitle',
                          data: {
                                    data:title,
                                    num:numVideos
                                },
                          });
}

function readTitle() {
                              fetch('./title.txt')
                              .then(response => response.text())
                              .then(data => {
                                             $('#title').val(data) // set title data
                                         //    $('#title').show() // show title
                                            });

                                            fetch('./numVideos.txt')
                              .then(response => response.text())
                              .then(data => {
                                             $('#countInfo').val(data+" videos found") // set title data
                                         //    $('#title').show() // show title
                                            });   

}

 function saveTxt(data){


                        $.ajax({
                            type: 'POST',
                            url: '/saveEporner',
                        
                            data: {
                                    data:data
                                  
                                  },
                        });
}
</script>

</body>
</html>